import { Button } from "@/components/ui/button";

const ExampleScreen = () => {
  return (
    <main>
      <p>스크린 예시 (예: 메인화면, 로그인 화면 등)</p>
      <Button></Button>
    </main>
  );
};

export default ExampleScreen;
